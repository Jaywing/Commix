namespace Commix.Pipeline.Model.Processors
{
    /// <summary>
    /// Model pipeline processor used to generate the schema used for mapping.
    /// </summary>
    public interface ISchemeGenerator : IProcessor<ModelContext, ModelProcessorContext>
    {

    }
}